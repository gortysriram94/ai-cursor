name: Build & Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version tag (e.g. v0.3.0)"
        required: true
        default: "v0.3.0"

env:
  VERSION: ${{ inputs.version || github.ref_name }}
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  # ── Windows ────────────────────────────────────────────────────────────────
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install pyinstaller pillow requests pyautogui pyperclip pynput psutil pywin32 pdfplumber

      - name: Download Ollama
        run: |
          Invoke-WebRequest -Uri "https://github.com/ollama/ollama/releases/latest/download/ollama-windows-amd64.zip" -OutFile ollama.zip
          Expand-Archive ollama.zip -DestinationPath ollama_tmp -Force
          New-Item -ItemType Directory -Force -Path ollama | Out-Null
          Copy-Item ollama_tmp\ollama.exe ollama\ollama.exe
          Write-Host "ollama.exe: $([math]::Round((Get-Item ollama\ollama.exe).Length / 1MB, 1)) MB"

      - name: Build with PyInstaller
        run: pyinstaller pushpa.spec --noconfirm

      - name: Build installer with Inno Setup
        run: |
          $ver = "${{ env.VERSION }}".TrimStart("v")
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss /DMyAppVersion=$ver
          Write-Host "Installer: $((Get-Item dist\AIcursor-windows-setup.exe).Length / 1MB) MB"

      - uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: dist/AIcursor-windows-setup.exe

  # ── macOS ──────────────────────────────────────────────────────────────────
  build-macos:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Install Python 3.11 via Homebrew
        run: |
          brew install python@3.11
          echo "$(brew --prefix)/opt/python@3.11/bin" >> $GITHUB_PATH

      - name: Verify Python
        run: python3.11 --version

      - name: Install dependencies
        run: |
          python3.11 -m pip install --upgrade pip
          python3.11 -m pip install pyinstaller pillow requests pyautogui pyperclip pynput pdfplumber
          python3.11 -m pip install pyobjc-core pyobjc-framework-Cocoa pyobjc-framework-Quartz \
            pyobjc-framework-ApplicationServices 2>/dev/null || echo "pyobjc skipped"

      - name: Download Ollama
        run: |
          mkdir -p ollama
          curl -L "https://github.com/ollama/ollama/releases/latest/download/ollama-darwin" -o ollama/ollama
          chmod +x ollama/ollama
          echo "ollama: $(du -sh ollama/ollama | cut -f1)"

      - name: Build with PyInstaller
        run: python3.11 -m PyInstaller pushpa_macos.spec --noconfirm

      - name: Verify app was created
        run: ls -la dist/

      - name: Package as DMG
        run: |
          brew install create-dmg
          create-dmg \
            --volname "AI Cursor" \
            --window-size 600 400 \
            --icon-size 100 \
            --icon "AIcursor.app" 175 190 \
            --hide-extension "AIcursor.app" \
            --app-drop-link 425 190 \
            "dist/AIcursor-macos.dmg" \
            "dist/AIcursor.app" || echo "create-dmg done (non-zero is normal)"

      - name: Zip as fallback if DMG missing
        run: |
          if [ ! -f "dist/AIcursor-macos.dmg" ]; then
            cd dist && zip -r "AIcursor-macos.zip" AIcursor.app
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: macos-build
          path: |
            dist/AIcursor-macos.dmg
            dist/AIcursor-macos.zip

  # ── Publish ────────────────────────────────────────────────────────────────
  publish:
    needs: [build-windows, build-macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: windows-build
          path: dist

      - uses: actions/download-artifact@v4
        with:
          name: macos-build
          path: dist

      - name: List release files
        run: ls -lh dist/

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.VERSION }}
          name: AI Cursor ${{ env.VERSION }}
          body: |
            ## AI Cursor ${{ env.VERSION }}

            ### Windows
            1. Download `AIcursor-windows-setup.exe`
            2. Run the installer — it sets up Start Menu + Desktop shortcuts
            3. On first launch the app downloads the AI model (~1.3 GB) automatically
            4. Press **Alt+A** on any text to start

            ### macOS
            1. Download `AIcursor-macos.dmg`
            2. Open the DMG, drag **AI Cursor** to Applications
            3. On first launch the app downloads the AI model (~1.3 GB) automatically
            4. Press **Option+A** on any text to start
          files: dist/*
          make_latest: true
